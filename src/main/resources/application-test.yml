server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:sales;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=true
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console